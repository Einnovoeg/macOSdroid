import Foundation

/// Centralizes external links that appear in both the app UI and the repository docs.
enum ProjectLinks {
    static let support = URL(string: "https://buymeacoffee.com/einnovoeg")!
}
